命令模式切换到末行模式：
	进入末行模式：按下  : 或者 /
	退出末行模式：按一下esc 或者 按两下esc 或者 删除末行中的全部命令

保存操作（write）：
	输入： ":w"		保存文件
	输入： ":w 路径"		另存为

退出（quit）:
	输入： ':q'		退出文件

保存冰退出：
	输入： ':wq'		保存并退出

强制（！）
	输入： ":q!"  表示强制退出，刚才做的修改不做保存

调用外部命令：
	输入： ":! 外部命令"
	#:! -la

搜索/查找
	语法： /关键词
	#/sbin
	切换上一个、下一个结果： N/n   (next)
